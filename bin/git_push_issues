#!/bin/bash 

set CURRENT=`git br | grep '*' | awk '{print $2}'`
for x in `git br | grep SFM| sed -e 's/\*//g'`; do echo; echo "* $x"; git checkout $x; git pull origin $x ;  git push origin $x; done

git checkout $CURRENT
